# Configuration for FedAvg baseline

mode: "FedAvg"

training:
  batch_size: 64
  local_epochs: 1
  learning_rate: 0.001
  hidden_size: 64

fl:
  rounds: 20

privacy:
  dp_enabled: false

compression:
  enabled: false
